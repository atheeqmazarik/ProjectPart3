<!--Navigation bar-->
<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container-fluid">
    <a class="navbar-brand" href="/"><i class="fa-solid fa-tv"></i> CourseHUB</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav ms-auto mb-2 mb-lg-0">

        <li class="nav-item">
          <% if (title == "Home") { %>
            <a class="nav-link active" aria-current="page" href="/home">
          <% } else { %>
            <a class="nav-link" aria-current="page" href="/home">
          <% } %>
            <i class="fa-solid fa-house"></i> Homepage</a>
        </li>

        <li class="nav-item">
          <% if (title == "Course List") { %>
            <a class="nav-link active" aria-current="page" href="/tvlist">
          <% } else { %>
            <a class="nav-link" aria-current="page" href="/tvlist">
          <% } %>
            <i class="fa-solid fa-list-ul"></i> Course List</a>
        </li>

        <li class="nav-item">
          <% if (title == "About") { %>
            <a class="nav-link active" aria-current="page" href="/about">
          <% } else { %>
            <a class="nav-link" aria-current="page" href="/about">
          <% } %>
            <i class="fa-solid fa-circle-info"></i> About</a>
        </li>

        <li class="nav-item">
          <% if (title == "Contact Us") { %>
            <a class="nav-link active" aria-current="page" href="/contact">
          <% } else { %>
            <a class="nav-link" aria-current="page" href="/contact">
          <% } %>
            <i class="fa-solid fa-address-card"></i> Feedback/Contact</a>
        </li>

        <% if (typeof displayName === 'string' && displayName) { %>
          <li class="navbar-text">
            <i class="fas fa-lg fa-user"></i> Welcome <%= displayName %> </li>
          <li class="nav-item">
            <a href="/logout" class="nav-link">
              <i class="fas fa-lg fa-sign-out-alt"></i> Logout</a>
          </li>
        <% } else { %>
          <li class="nav-item">
            <a href="/login" class="nav-link">
              <i class="fas fa-lg fa-sign-in-alt"></i> Login</a>
          </li>
        <% } %>
      </ul>
    </div>
  </div>
</nav>
